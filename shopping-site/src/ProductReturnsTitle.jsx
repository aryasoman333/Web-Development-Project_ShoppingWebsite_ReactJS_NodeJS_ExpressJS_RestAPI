import React from 'react';

const ProductReturnsTitle = ({ productDetails }) => {   
  return (
    <div className="details-titles">
      Returns
    </div>         
  );
};

export default ProductReturnsTitle;