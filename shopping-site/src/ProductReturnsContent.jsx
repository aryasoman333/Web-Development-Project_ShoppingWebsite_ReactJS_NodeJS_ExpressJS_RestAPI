import React from 'react';

const ProductReturnsContent = ({ productDetails }) => {   
  return (
    <div>{ productDetails.returns }</div>          
  );
};

export default ProductReturnsContent;